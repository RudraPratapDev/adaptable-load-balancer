# Core dependencies for the load balancer project
requests>=2.28.0

# Optional dependencies for network testing
# Note: mininet requires system-level installation and is not installable via pip on all platforms
# For Ubuntu/Debian: sudo apt-get install mininet
# For development without mininet, the project will work but skip network topology tests

# Development and testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0

# For enhanced logging and configuration
pyyaml>=6.0

# Research data generation and visualization
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.6.0
seaborn>=0.12.0
tabulate>=0.9.0